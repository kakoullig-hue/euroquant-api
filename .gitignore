# ── Secrets — NEVER commit these ──
.env
.env.local
*.key
*.pem
secrets/
.env.production

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.Python

# ── JS / Node ──
node_modules/
# Render demo build output (generated by render.yaml buildCommand)
/public/
web/dist/
web/.vite/

# ── OS / Editor ──
.DS_Store
.idea/
.vscode/
*.swp

# ── Logs & temp ──
*.log
reports/*.pdf
tmp/
*.tmp

# ── Keep the template ──
!.env.example
