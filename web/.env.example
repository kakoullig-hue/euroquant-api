# Copy to .env.local and fill in. NEVER commit real keys (web/.gitignore covers .env*).
# API base — defaults to the live Render deployment when unset.
VITE_API_BASE=https://euroquant-api.onrender.com
# Gateway API key (X-API-Key header). Without it the UI runs in demo-only mode.
VITE_API_KEY=
